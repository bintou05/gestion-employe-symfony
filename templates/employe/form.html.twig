    {% extends 'base.html.twig' %}

    {% block customStyleSheets %}
        #employe_isArchived{
            display:flex;
            gap:1.5rem;
        }
    {% endblock %}
    {% block title %}Enregistrement d'un employe{% endblock %}
    {% block body %}
    <div class="container mt-4">
        <div class="d-flex justify-content-between align-items-center mb-3">
            <h3>Enregistrement d'un employe</h3>
        </div>

        <!-- Tableau des employés -->
        <div class="card border-0 shadow-sm">
            <div class="card-body">
             {{ form(formEmp)}}
              
             </div>
        </div>
    </div>
{% endblock %}

